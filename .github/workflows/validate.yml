name: Validate Stakeholder Routes Chile

on:
  push:
    paths:
      - "data/**"
      - "docs/**"
      - "sources.csv"
      - "scripts/**"
      - ".github/workflows/validate.yml"
  pull_request:
    paths:
      - "data/**"
      - "docs/**"
      - "sources.csv"
      - "scripts/**"
      - ".github/workflows/validate.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/validate.py
